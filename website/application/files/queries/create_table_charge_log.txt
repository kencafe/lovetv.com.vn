CREATE TABLE IF NOT EXISTS `[[is_tables_name]]` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT,
  `requestId` varchar(128) DEFAULT NULL COMMENT 'ID của Request',
  `serviceName` varchar(128) DEFAULT NULL COMMENT 'Dịch vụ gửi quét cước',
  `packageName` varchar(128) DEFAULT NULL COMMENT 'Gói cước',
  `channel` varchar(50) DEFAULT NULL COMMENT 'Kênh phát sinh cước',
  `msisdn` varchar(50) DEFAULT NULL,
  `price` int(11) DEFAULT '0' COMMENT 'Giá cước',
  `originalPrice` int(11) DEFAULT '0' COMMENT 'Mức giá khi chưa khuyến mại',
  `event` mediumint(4) DEFAULT NULL COMMENT 'Event Status: 1 = Register, 2 = Cancel, 3 = Renew, 4 = Retry, 5 = Download, 6 = Buy',
  `descs` varchar(50) DEFAULT NULL,
  `active_date` datetime DEFAULT NULL COMMENT 'Thời gian KH xác nhận ĐK dịch vụ. ko phải thời gian gọi charging,  Format is yyyy-MM-dd hh:mm:ss (example: 2017-06-25 00:00:00).',
  `service_name` varchar(72) DEFAULT NULL COMMENT '-	servicename: Description about the Service (example: Xo So Mien Bac), max length is 30 character ',
  `register_channel` varchar(36) DEFAULT NULL COMMENT 'Kênh KH đăng ký, SMS, WEB ..vvv, khi renew vẫn là SMS hay WEB ..vvvv',
  `promotion` tinyint(1) DEFAULT NULL COMMENT '0: không free, 1: có free',
  `response` text,
  `status` tinyint(1) DEFAULT NULL COMMENT 'Trạng thái: 0 = Thành công, 1 = Thất bại',
  `day` int(11) DEFAULT NULL COMMENT 'Ngày, Ymd',
  `created_at` varchar(50) DEFAULT NULL COMMENT 'Thời gian, lưu timestamp',
  `logs` text,
  PRIMARY KEY (`id`),
  KEY `requestId` (`requestId`),
  KEY `serviceName` (`serviceName`),
  KEY `packageName` (`packageName`),
  KEY `msisdn` (`msisdn`),
  KEY `event` (`event`),
  KEY `status` (`status`),
  KEY `day` (`day`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COMMENT='Dữ liệu charge cước.';