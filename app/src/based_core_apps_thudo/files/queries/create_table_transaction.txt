CREATE TABLE `[[is_tables_name]]` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT,
  `requestId` varchar(35) NOT NULL COMMENT 'Request ID Vinaphone',
  `dtId` int(11) unsigned DEFAULT '1' COMMENT 'Distribution Id, update theo Last MO Command ID',
  `serviceId` varchar(128) DEFAULT NULL COMMENT 'serviceId',
  `packageId` varchar(128) DEFAULT NULL COMMENT 'packageId',
  `moCommand` varchar(128) DEFAULT NULL COMMENT 'moCommand ghi nhận khi đăng ký dịch vụ',
  `msisdn` varchar(20) DEFAULT NULL,
  `eventName` varchar(128) DEFAULT NULL COMMENT 'eventName: Subscriber, Unsubscriber, Renew, Retry...',
  `status` tinyint(4) DEFAULT '0' COMMENT 'Status: 0: register OK, 1: register fail, 2: unregister OK, 3: unregister fail, 4: renew OK, 5: renew fail, 6: retry OK, 7: retry fail,8: register lại OK, 9: register lại ko thành công, 10: drop OK, 11: drop fail, 12: buy OK, 13: buy fail, 14: change ok, 15: change fail',
  `price` int(11) unsigned DEFAULT '0' COMMENT 'Số tiền đem đi charge',
  `amount` int(11) unsigned DEFAULT '0' COMMENT 'Số tiền charge thành công',
  `mo` varchar(128) DEFAULT NULL COMMENT 'Mo tương tác của người dùng hiện tại hoặc moCommand trong SubscriberInfo',
  `application` varchar(255) DEFAULT NULL COMMENT 'Ứng dụng phát sinh giao dich',
  `channel` varchar(100) DEFAULT NULL COMMENT 'Kênh phát sinh: SMS, WAP, ...',
  `username` varchar(127) DEFAULT NULL COMMENT 'Username',
  `userip` varchar(127) DEFAULT NULL COMMENT 'UserIP',
  `promotion` varchar(127) DEFAULT NULL COMMENT 'Số chu kỳ, ngày, tuần hay tháng miễn phí. Sẽ tự động gia hạn sau khi hết khuyến mãi.',
  `trial` varchar(127) DEFAULT NULL COMMENT 'Số chu kỳ, ngày, tuần hay tháng dùng thử. Sẽ gửi tin nhắn thông báo khi hết thời gian dùng thử, nếu khách hàng không hủy thì sẽ bị gia hạn.',
  `bundle` tinyint(1) DEFAULT '0' COMMENT 'Xử lý nếu Kịch bản kinh doanh có đề cập: 0: đăng ký gói bình thường, 1: đăng ký gói kiểu bundle (không trừ cước đăng ký, không gia hạn)',
  `note` varchar(255) DEFAULT NULL COMMENT 'Note fw từ nhà mạng',
  `reason` varchar(255) DEFAULT NULL COMMENT 'Reason',
  `policy` tinyint(1) DEFAULT '0' COMMENT 'Chính sách khi hủy gói, sẽ có định nghĩa đối với từng kịch bản sử dụng. Ví dụ: 0: hủy bình thường, 1: hủy gói bundle và thiết lập lại trạng thái gói trước khi đăng ký bundle',
  `type` tinyint(3) unsigned DEFAULT NULL COMMENT '(1: mua bundle, 2: đăng kí gói)',
  `extendType` tinyint(3) unsigned DEFAULT '1' COMMENT '(1: lần đầu, 2: lần sau)',
  `day` int(10) unsigned DEFAULT NULL COMMENT 'Ngày phát sinh giao dịch: Ymd, 20170602',
  `created_at` datetime DEFAULT NULL COMMENT 'Thời gian phát sinh',
  `logs` varchar(1000) DEFAULT '' COMMENT 'Log giao dịch',
  PRIMARY KEY (`id`),
  KEY `serviceId` (`serviceId`),
  KEY `packageId` (`packageId`),
  KEY `moCommand` (`moCommand`),
  KEY `msisdn` (`msisdn`),
  KEY `eventName` (`eventName`),
  KEY `status` (`status`),
  KEY `amount` (`amount`),
  KEY `channel` (`channel`),
  KEY `extendType` (`extendType`),
  KEY `day` (`day`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
